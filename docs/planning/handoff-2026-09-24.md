# Handoff: first package build (2026-09-24)

This is where the first build stopped, and what comes next. It covers the work in PR #1 (branch `claude/repo-structure-build-plan-9weiyb`). Delete this file, or rewrite it, once its open items are done.

## State

- **Approved spec:** `docs/superpowers/specs/2026-09-23-gridcue-package-design.md`.
- **Approved plan:** `docs/superpowers/plans/2026-09-23-gridcue-package.md`. All 20 tasks are built.
- **Reviews:** each task was reviewed, then a final review covered the whole branch, then a four-batch fix wave ran and was re-reviewed. The re-review's two Important findings are fixed.
- **Gate:** `pnpm check` exits 0 from a clean frozen install. It reports:
  - 203 tests passed and 1 skipped. The skip is the live Jev test, which runs only with a key.
  - Evals: 9 exact, 2 safe abstentions, 4 rejected, 0 mismatch, 0 unsafe.
  - publint "All good!".
  - `gridcue/server` refuses to load under `browser` and under `worker` + `browser`, and loads under `workerd` and `edge-light`.
  - Browser bundles clean, including prerendered Next pages.
- **Browser smoke tests:** both examples were run in headless Chromium, in light and dark. Vite went 500 rows, then 89 after applying, then 500 after undo, then 57 with Ctrl/⌘+Enter. Next showed 5 group rows, then 285 rows after filtering, then the "risk score" clarification.
- **Local vs CI:** the local container runs Node 22, while CI and `engines` use Node 24.

## Owner decisions made during the build

- **Dark mode.** It applies to the whole page in both examples, following `prefers-color-scheme`, not just the command bar.
- **Workers support.** `gridcue/server` resolves to the real module under `workerd` and `edge-light`. `browser` keeps the refusing stub, and `worker` is deliberately absent. See ADR 0011. Whether `@typesafe-ai/sdk` runs under workerd is unverified until a real Wrangler build.
- **Confidence bands.** Families and enum/boolean values now use the same three bands as column picks: use the value at or above `ready`, ask between `clarify` and `ready`, drop it below `clarify`. Unsupported families at or above `clarify` are still refused, not confirmed.
- **Question budget.** The Jev provider asks about 27 questions per step on the 9-column wealth schema. With `maxQuestions` 96, that allows about 3 steps per request. This is accepted for the MVP but must be extended; see the next section.
- **PR shape.** The owner asked for one PR for the whole first build.

## Next steps, in order

1. **Done: resolution quality (steps 1 and 2 of the first handoff).** Spec `docs/superpowers/specs/2026-09-24-resolution-quality-design.md`, plan `docs/superpowers/plans/2026-09-24-resolution-quality.md`, and ADRs 0012 and 0013.
   - The six first-run mismatches are fixed.
   - 81 live requests produce no wrong view. The set is the 25 core cases plus 56 live-only cases, most written before the rule they tested.
   - The question cap is 600, and the model is pinned to `jev-1.13.0`.
   - `pnpm eval:live -- --verbose` shows each case's scores.
   - **Fixed after the plan (fan-out grill Q1):** multi-level sorts and nested groups previewed every level but applied only the last. Consecutive sort or group parts are now levels of one operation, in the order named. An elliptical "then by X" part keeps its verb. The Preview says "Group by A, then B".

   **Findings for the confidence-defaults ADR and later work.** These are the live-only mismatches, all of which ask instead of applying:
   - **Middling families where a person would act.**
     - "Rank each account by its gain" gave sort 0.73.
     - "Show IRAs at Northgate" and "Which accounts are at Harborline?" gave filter 0.80 to 0.82.
     - "Get rid of the account column" gave hide 0.69.
     - "Remove the custodian column" scored no family at all.
     - Sets the evidence for lowering `ready`, or rewording the family questions. Either needs an ADR.
   - **Two values of one column that aren't Host-declared names.** "Show me the retirement accounts" means IRA and Roth IRA. One single-answer Choice per column can't return both. The docs' pattern is one Noul per value.
   - **Compound requests without a comma.** "Show accounts over $1M sorted by gain" stays one part. Normalizer candidate: split before "sorted by", "grouped by" or "ordered by".
   - **Row nouns that are also columns** ("households at Northgate", "top households first") ask "Did you mean Household?". That is safe, but noisy.
1b. **Done: per-part fan-out** (spec `docs/superpowers/specs/2026-09-24-fan-out-design.md`, ADR 0014).
   - One part can now filter and sort or group; "also" adds a level; "within" nests correctly.
   - Live results: 51 of 56 live-only cases and 49 of 50 fan-out cases are exact, with 0 wrong views.
   - `pnpm eval:live -- --without=<signal>` measures what each question is worth.
   - **Remaining findings, all of which ask:**
     - "retirement accounts" (two values of one column);
     - "Remove the custodian column" (no change type scored);
     - row nouns that are also columns ("largest households first");
     - a value after the verb ("Sort by gain for trusts").
2. **Extend the question budget.** Done in step 1: one call, a higher cap, and no questions for named values. A second call was rejected, because Jev prices per token and answers batched questions in parallel.
3. **Publish to npm.**
   - Confirm `gridcue` is still free on npm (ADR 0004 asks for a re-check).
   - Choose `0.1.0`.
   - Set up GitHub Actions with npm trusted publishing and provenance, so no token is stored.
   - Write `docs/operations/release.md`.
   - The owner does the npm account and repository-link steps.
4. **Write the Site spec and plan, then build and deploy it** (ADRs 0002, 0003 and 0006). It covers the marketing page, a live demo on the Mock Provider, Fumadocs docs, and gridcue.dev on Cloudflare. Nothing may be scaffolded before the owner approves both. Deploying means creating the Cloudflare project, connecting the repo, pointing DNS, and running a real Wrangler build of a Worker that uses `gridcue/server`.
   - **Docs layout inspiration:** study kobra.systems, specifically its component library explorer UI and docs layout, not its components. Take its information architecture and interaction patterns: navigation, how a component page is laid out, and how live preview, code, and props sit together. Build our own implementation. Don't copy their source or assets unless their license allows it.

## Open follow-ups

**Not fixed, but deliberately safe as they stand:**

- `RowsAdapter.getState` returns the live object. Cloning it would break the `useSyncExternalStore` identity.
- Neither example disposes the controller on unmount. A simple `useEffect` cleanup would break under StrictMode's double mount, and the TanStack adapter also needs a `dispose`.
- In `apply()`, some adapter calls run before its try/catch: `getState`, `getCapabilities`, and `getDefaultState`. The same is true of `getState` in `undo()`. If one throws, the controller can still be left stuck on "applying". When `apply` throws partway through a write, nothing restores the previous view.
- Answering "No" to a family or value confirmation leads to a clarification with no options. It fails closed, and the User rephrases through "Edit request".
- Two value-clarification ids can collide when column or value ids contain dots. That is harmless, because one answer cannot confirm the other value.
- The TanStack adapter refuses OR and nested filter trees. The compiler never emits them, but the Site docs should say so.
- The Host's `ConfidencePolicy` reaching audit events has no end-to-end test.

**Gaps against the spec:**

- The Next example has no developer panel, and the Vite panel lacks before and after View State (spec section 7).
- A provider timeout ends in "error", not in idle with the Utterance kept (section 9).
- The handler has no auth hook. The docs should tell Hosts to put it behind their own auth and rate limits.
- `examples/vite/server.ts` (`pnpm start`) does not load `.env`.

**Cosmetic:**

- `biome migrate` for the `recommended` key.
- Duplicated percent and currency formatting.
- `optionsFor` takes a string.
- Type `FAMILY_PHRASE` as `Record<Family, string>`.

## Rulings made during the build

Each ruling was made without the owner and lists its cost if wrong.

- **R1.** Build on the designated branch instead of a worktree, because the session rules require it. Cost if wrong: none.
- **R2.** Accept Node 22 locally against `engines >=24`, because CI runs 24. Cost if wrong: a Node-24-only API passes CI but fails locally.
- **R3.** Build `gridcue` before the registry tests, because they import the build. Cost if wrong: a false test result, caught in review.
- **R4.** Push only after checking each batch. Cost if wrong: none.
- **R5.** Choose models by task (haiku to transcribe, sonnet to integrate and review, opus for final reviews). Cost if wrong: extra fix rounds.
- **R6.** Set up `@shadcn/lint` through Oxlint, scoped to the Tailwind surfaces, with no rules yet. Cost if wrong: one config file and two dev dependencies to revert.
- **R7.** Run the fix wave as sequential batches in one checkout. Cost if wrong: time only.
- **Task 4.** Add a curly-quote normalisation test beyond the plan. Cost if wrong: one test.
- **Task 9.** Make `MVP_OPERATIONS` read-only, with each adapter handing out a copy. Cost if wrong: one spread per adapter.
- **Task 10.** Map unknown provider error codes to `PROVIDER_FAILED`. Cost if wrong: a third-party provider's own code is lost.
- **Task 12.** Drop Jev's own 0.5 cutoff so the compiler's policy decides, and fail closed on a missing Jev answer. Cost if wrong: larger payloads and more clarifications for a Host whose `clarify` is below 0.5.
- **Task 14.** Keep the Host's own TanStack filters on clear, reset, and undo. Cost if wrong: "clear filters" leaves a Host filter in place.
- **Task 16.** Make the boundary test read four import forms and cover the root entry. Cost if wrong: a false positive, fixed by widening one rule.
- **Task 17.** Give the registry's Undo button the press class. Cost if wrong: one class.
- **Task 19.** Git-ignore the `AGENTS.md` and `CLAUDE.md` that `next dev` generates. Cost if wrong: two ignore lines.
- **Task 20.** Make the leak check scan prerendered Next pages, and rewrite AGENTS.md's gate line. Cost if wrong: a false positive on a page that legitimately contains a forbidden string.
- **Fix wave.** Refuse unsupported families at middling confidence instead of confirming them. Fingerprint the View State rather than raw TanStack state. Have a provider's too-complex message name no limit. Check that `worker` + `browser` still refuses. Cost if wrong: each is a one-line revert with its test.
