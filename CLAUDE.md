# Claude repository entry point

Read and follow `AGENTS.md` first. It is the canonical repository instruction file.

If the repository has not yet been scaffolded, follow **Planning status** below. Otherwise use the context-routing table in `AGENTS.md` and load only the documents relevant to the current task.

## Planning status

The build plan is still being settled. `BUILD_AGENT.md` is the original proposal, not an approved work order. Before scaffolding, read `docs/planning/build-plan-grill.md` for the open decisions, and `CONTEXT.md` for the project's glossary. Do not start implementation until the owner approves a spec under `docs/superpowers/specs/` and a plan under `docs/superpowers/plans/`.
